"""
Pipeline package for Audience Segmentation Tool.

Submodules:
    text_processor  — NLP feature extraction (TF-IDF, dimensionality reduction)
    segmentation    — Clustering engine (K-Means, DBSCAN, profile generation)
"""
